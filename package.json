{
  "name": "development",
  "version": "1.0.0",
  "description": "Manage projects",
  "scripts": {
    "sync": "lerna exec --no-bail --stream -- $(pwd)/sync.sh",
    "deps": "lerna exec --no-bail --stream -- npm ci || npm i"
  },
  "author": "",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
